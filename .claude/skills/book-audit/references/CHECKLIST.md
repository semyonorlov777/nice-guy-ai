# Эталонный чеклист аудита программы

**Версия:** 1.5
**Последнее обновление:** 2026-04-18
**Эталон:** nice-guy v2 (полная переработка по скиллу book-to-modes v2)

---

## A. Режимы и промпты

- [ ] **A1:** Количество режимов ≥ 7 (5 архетипов + подвиды по паттерну размножения). Не считая Свободный чат и Разговор с автором
- [ ] **A2:** Каждый режим имеет `system_prompt` в `program_modes` (не fallback на `programs.system_prompt`)
- [ ] **A3:** Каждый промпт содержит секции: РОЛЬ, КНИГА, ПРАВИЛА ОТВЕТА, ЛОГИКА ДИАЛОГА, АНТИПАТТЕРНЫ
- [ ] **A4:** Секция ПРАВИЛА ОТВЕТА включает: лимит 60-80 слов, один вопрос за раз, scaffolding fading (4→2→0)
- [ ] **A5:** Секция АНТИПАТТЕРНЫ перечислена явно: стены текста, похвала личности, overdiagnosis, несколько вопросов за раз
- [ ] **A6:** Промпт содержит конкретные концепции из книги (не общие фразы, а термины автора)
- [ ] **A7:** Промпт содержит suggested replies с типами (голос пользователя, сопротивление, навигация)

## B. Welcome-сообщения

> **Два пути хранения welcome:**
> - **Структурированный (Hub/NewChatScreen):** `welcome_ai_message` + `welcome_replies` (JSON array) + `welcome_title/subtitle/mode_label`
> - **Классический (ChatWindow):** `welcome_message` (текст с «кавычками» для replies)
>
> Проверяй ОБА пути. Для Hub-режимов приоритет — структурированный путь.

- [ ] **B1:** Каждый режим имеет welcome: либо `welcome_ai_message` (структурированный), либо `welcome_message` (классический). Для Hub-режимов — проверять `welcome_ai_message`
- [ ] **B2:** Welcome следует шаблону: ЭМОДЗИ + НАЗВАНИЕ → ХУК → КАК РАБОТАЕТ → ЧТО ПОЛУЧИТЕ → SUGGESTED REPLIES
- [ ] **B3:** ХУК продаёт результат, НЕ описывает процесс (не «мы будем...», а «ты получишь...»)
- [ ] **B4:** Suggested replies — от первого лица, голос пользователя (не вопросы к AI)
- [ ] **B5:** Есть безопасный exit-reply («Мне сложно сформулировать» или аналог)
- [ ] **B6:** Welcome не длиннее ~300 слов (не стена текста)

## C. Техническая реализация

- [ ] **C1:** Все режимы имеют `mode_template` с уникальным `key`
- [ ] **C2:** Для каждого `route_suffix` существует `page.tsx` в `app/program/[slug]/(app)/`
- [ ] **C3:** Иконка каждого режима существует в `mode-icons.tsx` → `iconMap`
- [ ] **C4:** `sort_order` корректен: нет дублей, логичный порядок (диагностика → анализ → практика → свободный)
- [ ] **C5:** `access_type`: хотя бы 1 free режим (точка входа для конверсии)
- [ ] **C6:** Нет `enabled: false` режимов, которые отображаются в UI как «coming soon» больше 1 месяца
- [ ] **C7:** `chat_type` совпадает с `mode_template.key` для уникальных режимов

## D. Кросс-режимные связки

- [ ] **D1:** Промпты упоминают использование данных из других режимов (`{{cross_mode_data}}` или аналог)
- [ ] **D2:** В файле MODE_DETAILS или ретроспективе описаны связки: ПЕРЕДАЁТ → / ПОЛУЧАЕТ ←
- [ ] **D3:** Режимы-аналитики (Анализ) передают данные в режимы-практики (Ролевая, Воркшоп)
- [ ] **D4:** Нет «сирот» — каждый режим (кроме Свободного чата) либо ПЕРЕДАЁТ, либо ПОЛУЧАЕТ хотя бы одну переменную. Если режим изолирован — это red flag
- [ ] **D5:** Переменные из ПЕРЕДАЁТ одного режима реально используются в промпте ПОЛУЧАЕТ другого. Проверка: найти имя переменной (напр. `life_position`, `user_game_formula`) в тексте промпта-получателя — оно должно быть в секции КРОСС-РЕЖИМНАЯ СВЯЗКА или ЛОГИКА ДИАЛОГА
- [ ] **D6:** Сводная таблица связок (из MODE_DETAILS) совпадает с тем, что реально прописано в промптах. Если в таблице написано «Разбор игры → Выход из игры: user_game_formula», то промпт «Выход из игры» должен содержать инструкцию использовать `user_game_formula`

## E. Интеграция с тестом (если у программы есть тест)

### E.I — Данные и контекст
- [ ] **E1:** Результаты теста (`scores_by_scale`) инжектятся в контекст чата через `appendTestScores()` в `prepare-context.ts`
- [ ] **E2:** Маппинг «шкала → режим» задокументирован (какой режим для какой шкалы primary)
- [ ] **E3:** В промптах режимов есть инструкция: если есть данные теста → использовать для персонализации
- [ ] **E4:** AI не зачитывает баллы вслух. **Note (v1.4):** правило теперь хардкодится в `appendTestScores()` (см. `lib/chat/prepare-context.ts`). Дополнительное упоминание в промпте — необязательно

### E.II — Конфигурация (чеклист из PLATFORM_MAP)
- [ ] **E5:** `test_configs` — запись существует с заполненными `questions`, `scales`, `scoring`, `ui_config`, `interpretation_prompt`
- [ ] **E6:** `programs.test_system_prompt` заполнен (**без него API вернёт 404**)
- [ ] **E7:** `programs.features.test = true` (feature flag)
- [ ] **E8:** `landing_data.test` заполнен (секция теста на лендинге)
- [ ] **E9:** `mode_template` создан с `is_chat_based = false` и `route_suffix` начинается с `/test/` (иначе хаб не покажет статус «Пройден»)
- [ ] **E10:** `program_mode` создан и `enabled = true` (карточка на хабе)
- [ ] **E13 (v1.5):** `programs.hub_messages` содержит ключи `first`, `returning_test`, `returning_notest` (без этого AI-приветствие на хабе рендерится пустым золотым кружком). Визуально проверить `/program/<slug>/hub?hub_state=first` и `?hub_state=returning-notest` — текст виден
- [ ] **E14 (v1.5):** Визуальная проверка: `HistoryScreen` теста показывает заголовок из `testConfig.ui_config.welcome_title/welcome_badge`, а не захардкоженное название другой программы. Проверить на `?test_state=history-multi` (см. секцию I)
- [ ] **E15 (v1.5):** Визуальная проверка: карточка теста в секции «Инструменты» хаба отображается зелёным цветом (forced в коде `InstrumentList.tsx` — `program_modes.color_class` для тестов игнорируется). Если карточка теста сливается с чат-режимами по цвету — значит либо `is_chat_based=true` выставлено ошибочно, либо `route_suffix` не начинается с `/test/`

### E.III — Темы и сортировка (если есть program_themes)
- [ ] **E11:** `program_themes.test_scale_key` заполнен для тем, привязанных к шкалам теста
- [ ] **E12:** Сортировка тем по баллам теста работает (высший балл → тема первая)

## F. Интеграция с темами (если у программы есть program_themes)

> **Note (v1.4):** Колонка `recommended_route` была удалена из схемы. Темы реально роутятся через `welcome_*` поля (см. `lib/queries/themes.ts → themeToWelcomeConfig`) и навигируются на `/chat/new?topic={key}` (см. `components/hub/ThemeCardsGrid.tsx`).

- [ ] **F1:** `program_themes.welcome_ai_message` и `welcome_replies` заполнены — они формируют welcome-экран при клике на тему
- [ ] **F2:** Если у книги есть отдельные режимы под темы, рассмотреть `welcome_system_context` — он передаётся в `/chat/new?topic=` для контекста, или маршрут темы переопределён в коде
- [ ] **F3:** Маппинг тема → шкала теста (`test_scale_key`) логичен (тема «границы» → шкала boundaries, см. E11)

## G. Ретроспектива и документация

- [ ] **G1:** Файл `examples/{slug}.md` в book-to-modes существует и актуален
- [ ] **G2:** Содержит секции: что сработало, что изобретали, специфика книги, уроки
- [ ] **G3:** Файл `{SLUG}_SYSTEM_PROMPTS.md` существует (промпты как MD-файл для ревью)
- [ ] **G4:** Файл `{SLUG}_MODE_DETAILS.md` существует (детализация режимов)

## H. Качество контента промптов (глубокая проверка)

- [ ] **H1:** Персона AI соответствует типу книги (терапия = бережный наставник, бизнес = коуч)
- [ ] **H2:** Тон единообразен во всех режимах одной программы
- [ ] **H3:** Промпты не содержат хардкод на конкретного пользователя или ситуацию
- [ ] **H4:** Ролевые режимы имеют: setup (2-3 обмена), маркер обратной связи (💡), дебрифинг
- [ ] **H5:** Экзаменационные режимы имеют: адаптивную сложность, metacognitive calibration, фидбэк через концепции книги
- [ ] **H6:** Лекционные режимы используют сократовский метод (вопросы → размышление → дополнение), не монологи

## I. Дебаг UI-состояний (v1.5)

Паттерн: stateful-экраны в платформе поддерживают query-параметр `?<screen>_state=<value>` для переопределения автоматически вычисленной фазы. Используется для визуальной проверки всех состояний без подготовки данных в БД.

Применяется в:
- `?hub_state=first|returning-test|returning-notest` — хаб программы ([hub/page.tsx](app/program/[slug]/(app)/hub/page.tsx))
- `?test_state=welcome|history-single|history-multi` — welcome-экран теста ([TestCardFlow.tsx](components/TestCardFlow.tsx))

- [ ] **I1:** При аудите новой программы пройти все 3 hub-состояния через query-параметр и сделать скриншот каждого
- [ ] **I2:** Если у программы есть тест — пройти все 3 test-состояния на `/program/<slug>/test/<testSlug>?test_state=...`
- [ ] **I3:** При добавлении нового stateful-экрана (≥2 состояний) в платформу — добавить свой `?<name>_state=` override для визуального тестирования

---

## Лог изменений

| Версия | Дата | Что изменилось |
|--------|------|----------------|
| 1.0 | Март 2026 | Первая версия. Эталон: nice-guy v2 |
| 1.1 | 2026-03-26 | B1: уточнение — два пути хранения welcome (структурированный + классический). Аудит games-people-play показал что проверка только `welcome_message` даёт ложный ❌ |
| 1.2 | 2026-03-26 | D4-D6: глубокая проверка кросс-режимных связок — нет «сирот», переменные реально используются в промптах-получателях, сводная таблица совпадает с промптами. Кросс-связки — фундамент персонализации, поверхностная проверка D1-D3 недостаточна |
| 1.3 | 2026-03-29 | E: расширение секции «Интеграция с тестом» — 4→12 пунктов. Добавлены подсекции E.II (конфигурация: test_system_prompt, feature flag, mode_template, landing_data) и E.III (темы: test_scale_key, сортировка). E3: убрана ссылка на ISSP → generic «данные теста». SQL сбора данных: добавлены запросы test_configs, mode_template для теста, program_themes.test_scale_key |
| 1.4 | 2026-04-18 | По итогам первого UI-прогона обоих тестов: F-секция переписана (колонка `recommended_route` была удалена, темы реально роутятся через `welcome_*` поля и `/chat/new?topic=`). E4: правило «не зачитывай числа» теперь хардкодится в `appendTestScores()`, требование иметь его в каждом промпте снято. Найдено 2 runtime-бага которых чеклист не ловил: (a) хардкод-карта tool keys в hub/InstrumentList.tsx делала новые режимы «невидимыми» с хаба (отрефакторено в derivation), (b) страницы quiz/theory в nice-guy читали несуществующее поле `config.quick_replies` и не показывали suggested replies (исправлено: читать `welcome_replies` с fallback). |
| 1.5 | 2026-04-18 | Второй UI-прогон preview нашёл ещё 2 невидимых чеклисту бага: (a) `programs.hub_messages = {}` у GPP → пустой золотой кружок на месте AI-приветствия хаба (добавлен E13), (b) HistoryScreen теста хардкодил «Индекс Синдрома Славного Парня» и «Диагностика» — для любой второй программы с тестом заголовок был чужой (добавлен E14, код фиксит через `testConfig.ui_config`). Новая секция I: паттерн `?<screen>_state=` для визуальной проверки stateful-экранов (hub_state, test_state). |
