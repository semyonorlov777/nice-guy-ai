# Мини-проекты — изолированы от основного приложения.
# Не индексировать при дефолтных Glob/Grep по корню.
# Подробности — в корневом CLAUDE.md (секция «Мини-проекты»).
mini/

# Worktree-копии (Claude Code создаёт их для агентов).
.claude/worktrees/
